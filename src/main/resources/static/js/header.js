// const Link = window.ReactRouterDOM.Link;

function Header () {

    return (
        <div>
            Header
            {/*<Link to='/profile' component={Profile} >Profile</Link>*/}
        </div>)
}